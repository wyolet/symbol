name = "fastapi"
category = "web"
detect = { deps = ["fastapi"] }

[checker]
exclude = ["docs_src/**"]

[checkers.side_effects.calls]
skip = ["FastAPI", "APIRouter", "lifespan"]

[checkers.side_effects.patterns]
debug = ["dependencies.py", "lifespan.py", "middleware.py"]
